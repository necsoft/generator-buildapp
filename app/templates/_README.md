Este es un proyecto creado con generator-buildapp, para poder ejecutarlo hay que hacer lo siguiente:

```
// nw = node-webkit alias
grunt && nw .
```

Adicionalmente los proyectos cuentan con un modo "devmode", para ejecutarlo:
```
grunt && nw . devmode
```
